version: 2
updates:
  # Root workspace dependencies
  - package-ecosystem: "bun"
    directories: ["/", "packages/*"]
    schedule:
      interval: "weekly"
    groups:
      patch:
        update-types:
          - "patch"
    cooldown:
      default-days: 14