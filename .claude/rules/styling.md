---
paths:
  - "**/*.tsx"
  - "**/*.css"
---

# Styling Conventions

- Merge classes with `cn` from `ui-design-system` (the codebase uses it in place of `clsx`)
- Import UI components from the design system: `import { Button, Modal, cn } from 'ui-design-system'`
